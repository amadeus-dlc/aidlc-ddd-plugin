# DDDプラグインのコンテキスト

[English](CONTEXT.md) | 日本語

このプラグインは、チームのドメインモデル、実装規約、検査の証跡を結び付けます。以下は、対応言語に共通する契約の用語です。

## 用語

**正規ドメインモデル**:
業務概念、その安定した識別、不変条件、操作、想定される失敗を定義する唯一の正です。
_避ける表現_: 言語固有のコードモデルを、そのまま業務の定義として扱うこと。

**実装写像**:
正規の業務定義と、その実装要素との対応です。業務の識別を参照し、定義し直しません。

**共通検査契約**:
対応言語をまたいで、規約への適合、規約違反、検査不能を区別する条件と証跡です。

**検査不能**:
必須の証跡を確立できない検査結果です。確認された違反とは区別し、規約への適合を示す結果にも使いません。

**メソッド固有のDomain Error**:
一つの業務操作または生成操作に所属する、想定された業務上の失敗です。その操作のエラー契約には、自身に宣言された失敗だけを含めます。

**生成規約**:
共通のドメイン規約と検査契約を満たす実装を生成するために、選択した規則です。

**実行ホスト**:
実行環境とレイヤー別の業務パッケージを、組み立てと委譲によって接続するアプリケーションの境界です。業務判断は、それを所有するパッケージに置きます。

**集約実行モデル**:
集約が操作を受け付け、実行する方式です。ソース表現と永続化方式から独立しています。

**ドメインのコード表現**:
対応言語でドメイン概念と業務操作を表す方式です。この選択で集約実行モデルを決めることはありません。

**Infrastructureレイヤー**:
このプラグインのアーキテクチャで、言語拡張のための機能を提供する層です。保存や外部サービスへのアダプタはインターフェイスアダプタ層に置きます。

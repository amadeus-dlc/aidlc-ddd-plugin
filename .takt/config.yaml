# このリポジトリのプロジェクト層の takt 設定。
#
# レート制限時の切り替え先 (rate_limit_fallback) は置かない。すべてのステップが Claude Opus 5.5 で動き、
# Claude Opus 5 と Claude Fable 5.1 は使わないため、切り替え先の候補が無い。制限に当たったステップは
# 中断するので、`takt list` の Requeue で止まった位置から再開する。

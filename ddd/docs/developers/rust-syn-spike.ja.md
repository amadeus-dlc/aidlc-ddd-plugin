# Rust＋synの構文解析試作

[English](rust-syn-spike.md) | 日本語 | [開発者向け文書](README.ja.md)

検証日: 2026-09-13。現行実装の比較基準: `83fadbfb8ac1100637dd157bc55621eae441da02`。

2026-09-23にT-10-02の内容へ更新した。規則`a`と`d`はtree-sitterではなくネイティブ抽出器の事実で判定するようになったため、以下の比較表は各抽出器が報告する内容とゲートの判定を分けて記載する。

2026-09-24にT-10-03の内容へ更新した。`ddd-rust-domain`が報告するすべての規則（`a`・`b`・`c`・`d`・`g`・`domain-packaging.*`）が、それらの規則が土台とする宣言索引、型解決、依存辺、パッケージ・モジュール解決とともに、ネイティブ抽出器の事実で判定するようになった。残る境界は[撤去で手放した範囲](#撤去で手放した範囲)、変わった判定は[判定が変わった範囲](#判定が変わった範囲)に記載する。

2026-09-24にT-10-04の内容へ更新した。残っていた4件の宣言列挙（`h`・`l`・`m`・`n`）が`protocol_version` 6のもとでネイティブ抽出器の事実で判定するようになり、`ddd-rust-use-case`と`ddd-rust-interface-adapter`が報告するすべてのrule_idがその事実で判定される。これにより、出荷されるRustソースセンサーのうちモジュール配置検査以外はすべて共通契約に載った。残る境界と変わった判定は、上と同じ2つの節に記載する。

2026-09-25にT-10-06の内容へ更新した。tree-sitter資産を撤去した。`lib/rust/analyzer.ts`、同梱の`web-tree-sitter`ビルド、`tree-sitter-rust`の文法はいずれも存在しないため、下表のtree-sitter列はこのリポジトリからは再生成できない。同列は2026-09-13時点の実測記録として残すもので、読み手が再実行できる内容ではない。従来の「tree-sitterから読み続ける範囲」節は[撤去で手放した範囲](#撤去で手放した範囲)に置き換え、移した2つの答えと、ここでは恒久的に行えなくなった比較を記載する。`unverified`の各変更をどのコマンドの出力から読んだかは[この報告が実測した範囲](#この報告が実測した範囲)に記載する。

**T-09/T-10のRust構文解析基盤として、Rust＋synを採用する。** 試作を踏まえてユーザーがこの方針を承認した。試作でDDD検査に使う情報を抽出でき、別ディレクトリへ移したネイティブ実行ファイルも動作した。現行センサーが公開タプルフィールドを見落とす2ケースも再現できた。本番への置き換えには、[参照解決と共通契約の設計](inspection-contract-design.ja.md)の実装・検証、全ルールの互換検証、対応環境ごとの配布検証が必要である。試作は導入済みセンサーを置き換えておらず、T-09/T-10も未完了である。

## 比較範囲を限定して検証した

[試作](../../experiments/rust-syn/)はsyn 3.0.5をCargo.tomlで固定し、Cargo.lockも管理する。この比較を行った時点の現行実装はBun／TypeScriptとtree-sitter-rustのWASMを使い、両方に同じソースを渡していた。T-10-06以降は現行実装がこの抽出器自身であるため、この比較は現在の比較ではなく過去時点の実測である。[検証スクリプト](../../scripts/verify-rust-syn.ts)で31ケースを検査した。このうち実際のドメインセンサーとの比較は5ケースで行い、別途6種類の不正なプロトコル入力を拒否することも確認した。31ケースの検証成功は、31本のRustコードが正しいという意味ではない。意図的なコンパイルエラーや検査不能ケースを含む。

試作で実装した範囲:

- 公開フィールドのルール`a`について、名前付き・タプル構造体の候補を抽出する。制限付き公開、raw識別子、Unicode、行番号を扱う。
- impl・trait・自由関数のシグネチャ、レシーバー形式、戻り値の型表記、enumのバリアント、型別名、import、モジュール、メソッド呼び出しを抽出する。
- `return self.amount;`を含め、フィールドを返すメソッドの構文形状を識別する。getter呼び出しのルール`d`全体や、副作用の有無は検証しない。
- テストしたマクロ、属性・derive、条件付きコンパイル、外部モジュール、不正構文、union、未対応修飾子を検査不能として報告する。

実センサーとの比較対象は`a`と`d`である。通常の名前付きフィールドと末尾式getterの合否は、従来の挙動と一致した。センサー全体の同等性、正規成果物のスキーマ、コマンド・生成時エラーの集合、所有に伴う効果、業務不変条件は、この試作の対象外である。

## 2つの抽出器とゲートの判定は別の問いに答える

`ddd-rust-domain`が報告するすべての規則は、ネイティブ抽出器の事実で判定する（`a`と`d`はT-10-02、残りはT-10-03）。したがって下表のtree-sitter列はその抽出器自身の出力であり、T-10-02以降はゲートの判定ではなかった。T-10-06でその抽出器を撤去したため、同列はこのリポジトリに存在しなくなった実装の2026-09-13時点の実測であり、ここでは再生成できない。判定列は、現在`ddd-rust-domain`がその入力に対して報告する内容である。

| 入力 | tree-sitter抽出器 | ネイティブ抽出器 | `ddd-rust-domain`の判定 |
|---|---|---|---|
| 非公開の名前付きフィールド | `a`のフィールドなし | `a`のメンバーなし | `a`の指摘なし |
| `pub amount: u64` | `a`のフィールドを抽出 | `a`のメンバーを抽出 | `a`を指摘 |
| `pub struct Invoice(pub u64);` | `a`のフィールドなし | `a`のメンバーを抽出 | `a`を指摘 |
| `pub struct Invoice(pub(crate) u64, u64);` | `a`のフィールドなし | `a`のメンバーを抽出 | `a`を指摘 |
| 末尾式`self.amount`のgetter | getterの構文を認識 | getterの構文を認識 | 呼出行で`d`を指摘 |
| `return self.amount;`のgetter | getterの構文を見落とす | getterの構文を認識 | 呼出行で`d`を指摘 |

判定が変わったのは、公開タプルメンバー2件と明示的`return`のgetter 1件の計3行である。検証スクリプトはこの3件と、判定が変わらない対照2件を実センサーへ通し、ケースごとの両方の件数を[実行記録](evidence/rust-syn-spike.json)の`actual_sensor_comparisons`に残す。試作のどのケースを実センサーへも通すかは、検証スクリプトの`SENSOR_EXPECTATIONS`から読む。従来その選択に使っていた空の`treeSitterFields`と`treeSitterGetters`は、それらを埋めていた抽出器とともに削除し、記録する各ケースの`tree_sitter_fields`と`getter_comparison`の列も同時に削除した。処理速度の優劣は比較していない。

## 撤去で手放した範囲

T-10-03では、`ddd-rust-domain`が判定に使う残りの入力をネイティブの事実へ移した。T-10-02の時点でtree-sitter側に残っていた3点は、いずれも移行済みである。

- 対象ファイルの集合。[`rust-modules.ts`](../../tools/ddd/lib/packaging/rust-modules.ts)の`mod`探索は、tree-sitterのモジュール配置ではなくネイティブのファイル単位の宣言からモジュールを解決するようになり、`moduleLayout()`は削除した。Cargoワークスペースの走査と層の割当は`Cargo.toml`から読むもので、元から構文解析器の担当ではない。
- 型の解決。[`program.ts`](../../tools/ddd/lib/rules/rust/program.ts)は`impl`ブロックと宣言の対応付けと呼び出しの受信型の解決をネイティブの事実から行い、tree-sitterの解析器からは何も参照しない。
- 呼び出し箇所。規則`d`の呼び出し、受信側の表記、リポジトリ引数の例外判定は、ネイティブの`calls`と`forwarded_argument_calls`から得る。転送の証明のために構文ノードを走査していたtree-sitterの`value-flow.ts`は削除した。

T-10-04では、残っていた4件の宣言列挙を移した。`h`はネイティブの`impls`と新設の`functions`、`l`はネイティブの`uses`、`m`はネイティブの`traits`と`types`、`n`はネイティブの`constructions`を読む。これにより`ddd-rust-use-case`と`ddd-rust-interface-adapter`が報告するすべてのrule_idが同じバッチで判定される。`i`はすでにネイティブの`calls`を`program.receiver`とともに読み、`k`は`edges.ts`がネイティブの`uses`から組み立てた依存辺だけを読んでいた。これらの列挙にネイティブ側の出所が無かった2つの事実、すなわちimplブロックの外で宣言された関数と、型・trait宣言が始まる行を、protocolが答えるようにした。

T-10-06で資産を撤去した。その解析結果から得ていた答えは2点残っており、それぞれ別の形で決着した。

- **どの申告ファイルを検査対象にするか。** 従来は申告ファイルをtree-sitterが解析できた場合にのみ評価していた。現在は検査がそのファイルを読めた場合に評価する。`InspectionTarget`は構文木の代わりに申告ファイル自身のパスを運び、読み取りが成功した位置でそれを設定する。読み取りに失敗した申告も`targets`に入るため、規則が判定すべきファイルであり続け、その実行では抽出器を必要とし続ける。そのファイル自身の宣言を持たないだけであり、ファイル単位の規則はそれについて何も報告しない。そうした申告についてゲートが報告し続けるのは、読み取りに依存しない内容、たとえばどのモジュールルートにも属していないことである。
- **ファイルのマクロ由来の未解析領域の位置。** `analyzer.macro-opaque`のnoteは無くなり、代替も作らない。これを生んでいた形式のうち、ネイティブ側で引き続き扱わないのは2つだけである。式の位置に書かれたマクロ（`let value = compute_amount!();`）と、組込みでない属性マクロ（`#[my_attr]`）である。verdictはこの2つについて何も述べなくなり、これらが無くても判定基盤が完全であることは示されていない。属性マクロは適用先の項目を置き換えられる。インラインモジュールに付けば、その中身を別の宣言や、走査が見ない`mod`へ書き換えうる。そのため、そこで規則が取り落とすものは「無い」ではなく未解決である。属性マクロの扱いは[#80](https://github.com/amadeus-dlc/aidlc-ddd-plugin/issues/80)で決める。代替のnoteを追加することは規則の新設にあたり、本変更の要求ではない。引き続き扱う形式は項目位置のマクロと`cfg`／`cfg_attr`である。モジュールを隠しうる位置ではその宣言に`domain-packaging.unresolved`として拒否し、それ以外は`domain-facts.unresolved`のnoteを添える。

撤去の代償は判定ではなく比較である。先行タスクが「置き換えた列挙がこの位置で何を報告していたかは確定できない」と記録した箇所——[判定が変わった範囲](#判定が変わった範囲)に挙げた、ネイティブの宣言走査が届かない位置——について、答えを出せたはずの文法は同梱の`tree-sitter-rust.wasm`としてのみ配布されていた。それが無くなったため、これらの問いはここでは保留ではなく、このリポジトリからは恒久的に答えられない。この前提で[完了タスク](completion-tasks.ja.md)が親課題へ引き継ぐ。

ネイティブ応答の限界のうち2点は、記録するだけで判定には使わない。deriveマクロは解析器が見ない項目を追加しうるが、注釈した宣言自身のメンバーは変えられないため、出現ごとの記録は行わない。属性マクロは注釈した宣言そのものを置換でき、展開前の宣言から展開後のメンバーは確定できない。現在の抽出器は属性マクロを記録しておらず、規則`a`・`d`の判定元に属性マクロが付いた場合も判定を止めない。この扱いは[#80](https://github.com/amadeus-dlc/aidlc-ddd-plugin/issues/80)で定める。項目マクロと`cfg`／`cfg_attr`は、モジュール宣言を隠している場合は`domain-packaging.unresolved`の所見として報告し、それ以外は`domain-facts.unresolved`のnoteとして判定に添える。これらの規則が判定する申告ファイルを持つ実行では、解析器が受理できなかったファイルをnoteで済ませるのは、判定元となるファイル（ゲートが検査する申告ファイルと、プログラム層の全クレートの全ソース）に含まれない場合に限る。含まれる場合はnoteでは済ませず、ゲートを検査不能として停止し、抽出器が返した理由を報告する。「このファイルは何も宣言していない」は、読めなかったファイルについて出してはならない唯一の答えだからである。

## 判定が変わった範囲

T-10-03では、既存のゴールデンケースと回帰試験の期待値をいずれも変更していない。`tests/golden/**`のケースと期待値は未変更で、試験は全件成功するため、カタログ済みのどの入力についても規則の`(rule_id, file)`の結論は動いていない。変わったのは、実行が判定そのものを返すかどうか、どの入口が抽出器を必要とするか、そしてカタログに無い入力について、配置検査の入口が検査できないモジュールについて何を報告するかである。

| 変化 | 移行から導かれる理由 | 判定箇所 |
|---|---|---|
| Rust規則の判定元ファイルが「申告ファイル＋**domain層**クレートの全ソース」から「申告ファイル＋**プログラム層の全クレート**の全ソース」へ広がった。use-case層またはinterface-adapter層のクレートに抽出器が読めないソースがある実行は、verdictを返さず終了コード127で停止するようになった。 | 型解決と宣言索引がネイティブになり、プログラム全体を対象とするようになったため、どのプログラムクレートに隠れた宣言でもプログラムの解決結果を変える。そのファイルを空として読むと、結論が黙って動く。 | [`context.ts`](../../tools/ddd/lib/rules/context.ts)の`requireDecisionBase` |
| `ddd-rust-use-case`と`ddd-rust-interface-adapter`も同じ条件で停止する。 | `ddd-rust-domain`とコンテキスト組み立てを共有するため、判定元も共有する。これらの規則自身の結論は変わらない。 | 同じ呼び出し |
| T-10-04が担当する規則のうち2件が、結論を変えずに判定入力を変えた。`i`はネイティブの`calls`を読み、`k`はネイティブの`uses`から組み立てた依存辺を読む。この変更の時点では`h`・`l`・`m`・`n`はtree-sitterの構文木から列挙し、解決だけをネイティブのプログラム解決と記号表で行っていた。これらが移ったのは次の行である。 | プログラム解決、記号表、依存辺は共有されているため、`ddd-rust-domain`のためにこれらを移したことで、それらを土台とする全規則の読み先が動いた。したがってT-10-04が引き継ぐ列挙は6件ではなく4件である。 | [`evaluators.ts`](../../tools/ddd/lib/rules/rust/evaluators.ts)、[`edges.ts`](../../tools/ddd/lib/rules/rust/edges.ts) |
| **T-10-04**: 残る4件の列挙が判定入力を変え、カタログ済みのすべての入力について結論は変わらなかった。`h`はネイティブの`impls`と`functions`、`l`はネイティブの`uses`、`m`はネイティブの`traits`と`types`、`n`はネイティブの`constructions`を読む。ゴールデンケースと回帰試験の期待値はいずれも未変更で試験は全件成功するため、カタログ済みのどの入力についても規則の`(rule_id, file)`の結論は動いておらず、`m`の各所見が運ぶ行も従来と同じである。カタログに無い入力について結論が動いた範囲と、同じ走査が届かない他の位置——従来の結論は本書では確定していない——は、次の行に記録する。 | これらの列挙にネイティブ側の出所が無かった2つの事実、すなわちimplブロックの外で宣言された関数と、型・trait宣言が始まる行を答えるようにした。いずれも、protocolがメンバー・モジュール・implメソッドで既に用いていた基準——属性ではなく最初のキーワード——で報告する。これはtree-sitterのノード範囲が持っていた基準と同じである。関数が立ちうる位置を宣言集合が取り落とすと、所見は黙った合格に変わる。次の行が記録するのは、まさにそうした位置の1つである。答えている位置は4つで、最上位、インラインモジュール内、別の関数の本体内、本体を書くtraitメソッドである。本体を持たないtraitメソッドとimplブロックのメソッドは、ここには重複して載せない。この4つは、自由関数が記録される位置のすべてではない。走査は関数をそれを保持する項目経由でたどるため、最上位の`const`・`static`の初期化ブロックや`impl`ブロックの関連定数の既定値に書かれた自由関数も、その項目経由で同じ`functions`に載る。一方、traitの関連定数の既定値に書かれたものには届かない。 | [`evaluators.ts`](../../tools/ddd/lib/rules/rust/evaluators.ts)、[`domain_facts.rs`](../../experiments/rust-syn/src/domain_facts.rs) |
| **T-10-04**のうちカタログに無い入力について、**traitの関連定数**の既定値に書かれたものが宣言の走査に届かなくなり、4件の列挙がいずれもその位置にあるものを取り落とすようになった。規則`n`はそこに書かれた構築——`pub trait D { const EMPTY: Invoice = Invoice { id: String::new() }; }`で、`Invoice`が記号表に載っている場合——を報告しなくなり、構築が取りうる`kind`の4値（`struct-literal`・`update-syntax`・`associated-call`・`default-call`）がいずれもこの位置で落ちる。そこのブロックが宣言する項目も同様に落ちるため、`const N: u64 = { struct FooRepository; 1 };`のリポジトリ構造体を`m`が報告しなくなり、同じ書き方の`use`を`l`が、そこに書かれた自由関数を`h`が報告しなくなる。そこで所見を出していた入力はいずれも合格になり、新たに所見が出る入力はない。隣接する2つの位置は影響を受けない。本体を書くtraitメソッド内の構築・項目と、`impl`ブロックの関連定数の既定値に書かれたものは、どちらも従来どおり記録され、従来どおり報告される。規則`a`もこの位置で影響を受けない。そのメンバーは、この走査ではなく、synが定義する全ノードを訪れる別のパスが記録するためである。 | tree-sitterの列挙はファイルの根から名前付きノードを全再帰したため、そこに何が書かれていても到達していた。ネイティブの走査は式をそれを保持する項目経由でたどり、`Item::Trait`のアームはtraitメソッドの既定本体と項目マクロにしか降りず、`TraitItem::Const`は`_`のアームに落ちる。そのため既定値の式も、そこのブロックが宣言する項目も走査されない。`Item::Const`・`Item::Static`・`ImplItem::Const`はそれぞれ自身の式を走査するため、隣接する2つの位置は所見を保つ。これらの入力はゴールデンケースにも回帰試験の期待値にも無いため、更新するベースラインは無く、この結論の変化は試験の期待値ではなく文書に記録する。この同じ経路——式をそれを保持する項目経由でたどること——は、同じ4件の列挙が見ない位置をほかにも残す。これらは結論が動いた範囲としてではなく、走査が届かない位置として記録する。以下は抽出器に対して確認できた位置であり、走査が止まる位置を網羅したものではない。`Item::Enum`のアームは`syn::Fields::Unit`を渡すだけで`node.variants`を読まないため、バリアントの判別値は走査されない。宣言が持つ型は範囲の文字列として記録する——フィールド、引数、戻り値、型別名の参照先、implブロックの対象型とtrait——ため、配列型`[T; expr]`の長さのように型構文の内部に書かれた式は走査されない。`generics`は型別名が型引数を取るかどうかを答えるために1度読むだけなので、constジェネリックの既定値も走査されない。式の側でも、パスの型引数は走査されない。`Expr::Path`は裸の識別子しかたどらず、`call`はturbofishが素の関連呼び出しではないと判定するためだけにセグメントの型引数を読み、`method_call`はturbofishを読まず、`Expr::Struct`はパスを文字列として記録する。そのため`Wrap::<{ struct FooRepository; 1 }>::new()`・`x.get::<{ ... }>()`・`Wrap::<{ ... }> { v: 0 }`のように式のパスへ書かれたconstジェネリック引数は走査されない。これらの位置でtree-sitterの列挙が何を報告していたかは、本書では確定していない。その文法定義はvendoredの`tree-sitter-rust.wasm`としてのみ提供されており、T-10-06でそれを撤去したため、これらの位置でどの名前付きノードを生成したかをリポジトリ内でもはや読み取れないからである。これらの不足はここでは閉じず、親課題向けに[完了タスク](completion-tasks.ja.md)へ記録する。規則`c`と`d`が同じバッチ（`constructions`・`impls`・`calls`）を読むため、走査範囲を広げるとT-10-04が引き継いでいない結論も動くからである。 | [`domain_facts.rs`](../../experiments/rust-syn/src/domain_facts.rs)の`Item::Trait`、[`evaluators.ts`](../../tools/ddd/lib/rules/rust/evaluators.ts)の`ruleH`・`ruleL`・`ruleM`・`ruleN` |
| protocolは`protocol_version` 6になり、5を答えるままの導入先は受け付けない。 | 応答に`functions`と、すべての型・traitの`line`が加わり、受理側はいずれも省略可能として読まない。これらを欠く応答は、そこに何も宣言していない応答と区別できないからである。置き換えられた側のprotocolに留まった抽出器は`native-extractor:protocol-mismatch`に分類し、これらの規則を運べない事実で判定せず、verdictを出さずに終了コード127で終わる。 | [`main.rs`](../../experiments/rust-syn/src/main.rs)、[`domain-facts/index.ts`](../../tools/ddd/lib/rust/domain-facts/index.ts) |
| `ddd-rust-module-layout`とCI用入口`ddd-check-rust-module-layout`が抽出器を必要とするようになり、起動できないとき、および応答を読めないときは、応答せず停止する。 | 両者が呼ぶモジュール走査がネイティブの宣言からモジュールを解決するため、いずれの条件でも走査の土台となる宣言が得られない。 | [`module-layout/check.ts`](../../tools/ddd/lib/module-layout/check.ts)の`readProjectFacts` |
| `ddd-rust-module-layout`とそのCI用入口で、`mod`宣言やCargoターゲット根のソースファイルがプロジェクト探索の対象外ディレクトリ（隠しエントリ、`aidlc`、`node_modules`、`vendor`、`target`、`dist`）内にある場合——直接その位置を指す場合も、シンボリックリンク経由でそこへ解決する場合も——`module-layout.unresolved`を宣言側の位置に報告するようになった。宣言側のファイルの該当行、ターゲット根ならそのクレートの`Cargo.toml`である。従来は追従していた。tree-sitterの走査がそのファイル自身を開いて配置まで判定していたため、規約どおりの名前なら所見は一切出ず、名前が違えばそのファイルに`module-layout.violation`が出ていた。そのため、そうしたファイルを規約どおりに置いて合格していたプロジェクトは、不合格になる。 | この走査がたどる宣言は同じ探索が集めた1回のバッチから来て、走査が読むファイルの名前で引かれるため、探索対象外のファイルは抽出器に問い合わせていない。読む対象はリンクを解決した先のファイルなので、この判定もリンク解決後に行う。同じ探索が対象外としたファイルの配置を判定することは元々矛盾しており、従来の走査はファイル自身を開くことでそれを覆い隠せていた。バッチにはそれができず、探索が対象外としたツリーはこのプロジェクトが判定する対象でもない。 | [`rust-modules.ts`](../../tools/ddd/lib/packaging/rust-modules.ts)の`covers`、[`module-layout/check.ts`](../../tools/ddd/lib/module-layout/check.ts) |
| 同じ2入口で、宣言を読めなかったモジュールファイルは`module-layout.unresolved`のみを受け取り、`mod-rs`のプロジェクトで並んで報告されていた`module-layout.violation`が出なくなった。 | そのファイルが子を宣言するかどうかこそが読めなかった内容であり、`mod-rs`の配置規則には入力が無い。従来は「子を宣言していない」とみなして適用していた。 | [`module-layout/check.ts`](../../tools/ddd/lib/module-layout/check.ts) |
| ソースファイルの名前が`*.rs`でない`mod`宣言——Rustが受け付ける`#[path]`でそのようなファイルを指す場合も、シンボリックリンクがそこへ解決する場合も——は、モジュールを走査する各入口が、その宣言の位置に報告するようになった。`ddd-rust-domain`は`domain-packaging.unresolved`、配置検査の2入口は`module-layout.unresolved`である。tree-sitterの走査は名前にかかわらずそのファイルを開いていた。`ddd-rust-domain`はそれをRustとして読んでモジュールとして扱い、配置検査の2入口はそのファイルが置かれた場所で配置を判定し、`module-layout.violation`をそのファイルに報告していた。`ddd-rust-domain`では、その宣言が名付けるパッケージを集約マッピングが宣言していればそのモジュールは宣言済みとして扱われたため、従来合格していた実行が不合格になる。配置検査の2入口では所見の位置がそのファイルから宣言側へ移るだけで、実行は従来も今も不合格である。 | 各バッチは走査が読むファイルの`.rs`という名前で集める。プログラム層のクレートは`rustSourcesUnder`、配置検査の2入口はプロジェクト探索が集めるため、他の名前は抽出器に問い合わせていない。判定をリンク解決後に行うのはそのためである。そのまま追従すると、抽出器へ渡してすらいないファイルを「読めなかった」と報告し、`ddd-rust-domain`では tool-unavailable の終端で実行が終わっていた。 | [`rust-modules.ts`](../../tools/ddd/lib/packaging/rust-modules.ts)の`isRustSource`と`covers`、[`program.ts`](../../tools/ddd/lib/rules/rust/program.ts)、[`module-layout/check.ts`](../../tools/ddd/lib/module-layout/check.ts) |
| `#[path]`の扱いは、指す先が検査対象である限り変わらない。対象の`.rs`ファイルが1つに定まるものは従来どおり追従し、`cfg_attr`による条件付き、複数指定、不正リテラル、クレート外脱出のみを従来どおり`domain-packaging.unresolved`として報告する。 | T-10-03は、宣言の取得元を移しつつこの振る舞いを維持することを求められていた。 | [`rust-modules.ts`](../../tools/ddd/lib/packaging/rust-modules.ts) |

実行が停止するようになった行——判定元ファイルの拡大、それを共有する2つのセンサー、抽出器を必要とするようになった配置検査の2入口——は、いずれも「答えないこと」の変化であり、新しい所見ではない。所見が増える入力も減る入力もなく、従来正しい理由で合格していた実行は今も合格する。`i`と`k`の行は、結論を動かさずに判定入力だけを移したものである。報告される所見が実際に変わるのは4行——配置検査の2入口に関する2行、バッチが運ばないモジュールのソースファイルに関する1行、宣言の走査が届かない位置に関する1行——で、それぞれ変わる入力を明示している。最後の1行で報告される所見が変わるのは、その行が結論を述べている位置である。そのうち2行は、合格していた入力を不合格にもする。探索対象外ディレクトリの行は、従来そのファイルが置かれた場所で配置を判定していたためであり、名前が`*.rs`でないソースの行は、`ddd-rust-domain`に限り、従来そのファイルをRustとして読み、宣言が名付けるモジュールとして数えていたためである。走査が届かない位置の行だけは向きが逆で、traitの関連定数の既定値で従来`h`・`l`・`m`・`n`を指摘していた入力が合格になる。

## 構文解析だけでは名前や型を解決できない

synの公式資料は、Rustのトークンを構文木へ変換する役割を説明している。`parse_file`はソース文字列を受け取り、editionを指定する引数は持たない。利用側では、修飾子や非網羅的な列挙型、Verbatimノードなどの未対応構文も扱う必要がある。[syn API](https://docs.rs/syn/3.0.5/syn/)、[parse_file](https://docs.rs/syn/3.0.5/syn/fn.parse_file.html)、[ソース位置の取得](https://docs.rs/proc-macro2/latest/proc_macro2/struct.Span.html)を参照。

検証スクリプトはrustcと比較し、次の限界を実測した。

- 存在しない`MissingType`や、戻り値`u64`に文字列を返す関数も構文解析には成功する。rustcは拒否するため、構文解析の成功だけでは型に依存するDDD規則を判定できない。
- 型別名`Outcome<T>`、再公開されたエラー、`Self::Error`の表記は取得できるが、参照先の型は未解決のままである。アプリ独自の`Result<T, E>`も解析できるため、`Result`という名前だけでは標準の結果・エラー契約と判断できない。
- 型推論を要するレシーバーの呼び出しも解析できるが、その型の解決には対応していない。
- 同じ`async fn`のソースは両試行で解析できるが、rustcはedition 2015で拒否し、2018で受理する。対照用の単純な構造体は2015/2018/2021/2024でコンパイルできた。edition境界を調べた結果であり、全editionへの対応保証ではない。
- マクロが生成するフィールドは、展開前の構文には現れない。一方、偽になる`cfg`配下のフィールドは構文上は存在する。試作は両方を検査不能とし、候補を残しても違反確定には使わない。
- `mod line;`を持つ`invoice.rs`と`invoice/mod.rs`は、どちらも外部モジュールの読込が必要と報告する。子ファイルの探索や、プロジェクトのモジュール配置設定の検査は行わない。

Cargoのワークスペース・ターゲット・featureの把握、ファイルやクレートをまたぐimport・型別名、型引数の置換、traitの解決、マクロ展開、有効な`cfg`の選択は、別途設計する必要がある。`cargo check`の成功はコンパイラが受理した証拠になるが、独自DDD検査が必要とする解決済みシンボルを、それだけで取得できるわけではない。

## 検証した環境ではネイティブ実行ファイルを単独で動かせた

releaseバイナリを新しい一時ディレクトリへコピーし、PATHを空のディレクトリに向け、親の環境変数を引き継がずに実行した。実行時にCargo・rustc・Bun・tree-sitterは不要だった。検証したApple Silicon Macでは、シンボル除去後の実行ファイルは約1.86 MBで、動的リンク先はシステムの`libSystem.B.dylib`だけだった。入力のハッシュ、厳密なバージョン、容量・依存先、所要時間、各ケースの結果は[実行記録](evidence/rust-syn-spike.json)に残した。

試作のビルドにはRustツールチェーンと、初回はcrates.ioへのアクセスが必要になる。検証スクリプトにはBunも必要である。現在のプラグイン利用者にRustの導入を要求する変更ではない。

本番配布では、対応するOS・アーキテクチャを決め、各環境向けのバイナリをビルドして公開する必要がある。チェックサムと実行権限、新規導入・更新、バイナリ欠落時の扱いも検証する。センサー実行時のソースビルドは避ける。WASM化も配布方法の候補だが、今回は試していない。Linux、Windows、x86_64、最低OS・Rustバージョンは未検証である。プラグインの導入・承認処理との接続はこの一覧から外した。何を根拠に外したかは[この報告が実測した範囲](#この報告が実測した範囲)に記載する。

## この報告が実測した範囲

[実行記録](evidence/rust-syn-spike.json)の`unverified`は、この解析基盤について、どの実測でも答えていない問いの一覧である。項目がこの一覧から外れるのは実測した場合だけであり、「実測すればおそらく通る」という見込みでは外さない。T-10-06では1件を外し、1件を絞った。各変更をどのコマンドのどの出力から読んだかを次に示す。いずれも`darwin-arm64`での実行である。

| `unverified`の項目 | 変更 | 根拠にした出力 |
|---|---|---|
| `plugin installation and gate integration` | 削除 | `bun run test:install` — 51 pass、2 skip、0 fail（2件のskipは`DDD_VERIFY_REMOTE_INSTALL`を要するネットワーク導入）。`bun ../.codex/tools/aidlc-plugin-test.ts . --install .. --harness <claude\|codex>` — 両ハーネスで`Plugin test: CLEAN`、drops 0、グラフのコンパイル成功、2回目の合成も冪等。`bun test tests/t1-gate-integration.test.ts` — 332 pass、1 skip、0 fail。`bun scripts/verify-dist.ts` — ハーネスごとに387ケース、failed 0。実行対象は`dist/<harness>/tools`。`find dist/claude/tools dist/codex/tools -name '*.wasm'` — 出力なし、終了コード0。どちらのビルド済みtoolsツリーにも`*.wasm`は無い。 |
| `full sensor parity` | `sensor answers for inputs the golden catalog does not carry`へ絞った | `bun run check` — 47ファイルで2340 pass、3 skip、0 fail。`tests/golden/**`の期待値はいずれも変更していない。加えて上記の`bun scripts/verify-dist.ts`。この2つで、出荷されるすべてのRustセンサーが報告するすべてのrule_idがこの抽出器で判定されること、およびカタログ済みのどの入力も`(rule_id, file)`の結論を保つことは成立する。カタログに無い入力についての一致は成立せず、[判定が変わった範囲](#判定が変わった範囲)は実際に結論が動いた位置を記録しているため、残りは一覧に残す。 |

次の項目は残した。各実測がそれを扱えない理由を併記する。

- `Cargo and cross-file resolution`、`type inference and trait solving`、`macro expansion and cfg selection`、`source edition selection` — synはいずれも提供せず、このリポジトリにこれらを供給するものもない。実測するコマンドが存在しない。
- `Linux/Windows/x86_64` — 上記の実測はすべて`darwin-arm64`で行った。ここに収録されている`bin/manifest.json`のプラットフォームも`darwin-arm64`だけである。CIは`ubuntu-latest`で`bun run check`を実行し、そこでは`prepare:native`がそのランナー向けにビルドするが、その実行結果は本書に記録していない。Windowsの実行経路は存在しない。
- `minimum OS/Rust versions` — どちらも変えて測っていない。
- `WASM distribution` — WASM化は試していない。ネイティブ配布は成立しているが、これは別の配布方法であり、WASM化の根拠にはならない。

## 再実行して結果を確認する

リポジトリのルートから実行する。

```sh
cd ddd
bun run experiment:rust-syn
# 管理対象の実行記録を明示的に更新する場合:
bun run experiment:rust-syn --write
cargo fmt --manifest-path experiments/rust-syn/Cargo.toml --check
cargo clippy --locked --manifest-path experiments/rust-syn/Cargo.toml --all-targets -- -D warnings
```

このコマンドは`--locked`でビルドし、テスト入力と実センサーを比較する。コンパイラを対照にするケースではrustcを実行し、バイナリ移動後の動作と出力の再現性も確認してJSONを出力する。検証に失敗すると終了コードは非ゼロになる。検証スクリプトの`SENSOR_EXPECTATIONS`は、実センサーへも通す各ケースについてゲートの判定を記録し、どのケースを通すかもここから選ぶ。本番修正が実際に動かしたものがあれば、この期待値と本書を同じ変更で更新する。

試作実行ファイルは標準入力から1件のJSONリクエストを受け取る。以下のプロトコル1は試作専用である。センサーが読むのは[ネイティブ抽出器の配布](native-extractor-distribution.ja.md)に記載した`domain-facts`プロトコルであり、この形式ではない。

```json
{"protocol_version":1,"files":[{"path":"invoice.rs","source":"pub struct Invoice(pub u64);"}]}
```

出力には`field_inspection.state`（`pass`・`violation`・`unresolved`）、フィールド候補、構文情報、`semantic_analysis: "unsupported"`を含む。`pass`は限定されたフィールド検査だけの判定である。終了コード0は検査不能を含む報告の出力成功を意味する。不正なリクエストは終了コード2となり、標準エラーに理由を出し、標準出力は空にする。この形式は試作用で、現行センサーの判定契約や今後の共通スキーマとは異なる。synのノードをプロセス外へ渡さず、プロジェクト全体の合格判定も出さない。

## 共通設計へ反映する項目

[言語共通の契約](language-independent-design.ja.md)を設計の基準とする。T-09では[検査設計](inspection-contract-design.ja.md)に沿って、ルールごとに必要な情報、解決・完全性の状態、共通の判定処理と言語固有解析の境界を実装する。T-10-02では、公開タプルフィールドの2ケースと明示的なreturnを持つgetterを規則`a`・`d`で解消し、両モジュール配置で確認した。T-10-03では、`domain-packaging.*`が読むパッケージ解決・モジュール解決を含め、`ddd-rust-domain`の全体を共通の判定基盤へ載せた。T-10-04では、use-case／interface-adapterの規則も同じ基盤へ載せた。T-10-05では、モジュール配置検査とそのCI用入口を両方のプロジェクト配置で同じ事実により判定するようにしたため、出荷されるRustソースセンサーはすべてこの基盤で判定する。T-10-06ではtree-sitter資産を撤去したため、これがそのいずれも読む唯一の抽出器である。T-10に残るのは、ゴールデンカタログを超えるセンサー全体の互換性と、対応環境ごとの配布である。必須情報を解決できない場合は承認を止め、TypeScript Compiler API側も同等の業務シナリオで同じ契約を検証する。
